#include <iostream>
using namespace std;

void ah(void) {
	int hh, mm; cin >> hh >> mm;

	int time = 9 * 60,
		time2 = hh * 60 + mm;

	int time3 = time2 - time;

	cout << time3 << "\n";
}

int main(void) {
	ios::sync_with_stdio(false);
	cin.tie(nullptr); cout.tie(nullptr);

	ah();
	return 0;
}
