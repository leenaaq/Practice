#include <iostream>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    float s1, s2; cin >> s1 >> s2;

    if (s2 / 2 <= s1) cout << "E";
    else cout << "H";
    return 0;
}
