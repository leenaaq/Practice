#include <iostream>
using namespace std;

int main()
{
    int n, a[5], sum = 0; cin >> n;
    for (int i = 0; i < 5; i++) {
        cin >> a[i];
        if (a[i] == n) sum++;
    }
    cout << sum;
    return 0;
}
