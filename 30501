#include <iostream>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n; cin >> n;
    string a, b;

    for (int i = 0; i < n; i++) {
        cin >> a;
        for (int j = 0; j < a.length(); j++) {
            if (a[j] == 'S') {
                b = a;
                break;
            }
        }
    }

    cout << b;
    return 0;
}
