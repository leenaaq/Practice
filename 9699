#include <iostream>
#include <algorithm>
using namespace std;

int main() {
	int n, b;

	cin >> n;

	for (int i = 0; i < n; i++) {
		int a[5];
		for (int j = 0; j < 5; j++) {
			cin >> a[j];
		}
		int b = *max_element(a, a + 5);
		cout << "Case #" << i+1 << ": " << b << "\n";
	}
	
	return 0;
}
