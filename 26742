#include <iostream>

using namespace std;

int main() {
  string str;
  cin >> str;

  int ws = 0, bs = 0;

  for (char c : str) {
    if (c == 'B') ws++;
    else bs++;
  }

  int p = ws / 2 + bs / 2;
  cout << p << "\n";

  return 0;
}
