#include <iostream>
using namespace std;

int main(void) {
    string s;
    int cnt = 0;
    cin >> s;

    for (int i = 0; i < s.length(); i++) {
        if (s[i] != s[i + 1]) cnt++;
    }
    if (!cnt) cout << cnt;
    else cout << cnt / 2;
    return 0;
}
