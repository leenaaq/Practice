#include <iostream>

int main()
{
    int a[3];
    int b[2];

    for (int i = 0; i < 3; i++) std::cin >> a[i];

    b[0] = a[1] - a[0];
    b[1] = a[2] - a[1];

    if (b[0] > b[1]) std::cout << b[0]-1;
    else std::cout << b[1]-1;
 
}

