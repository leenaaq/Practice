#include <iostream>
using namespace std;

int main()
{
    int a, b, c = 0; cin >> a >> b;
    int sum = a + b;
    if (sum / 100 > 0) c += 3;
    else if (sum / 10 > 0) c += 2;
    else if (sum / 1 > 0) c++;
    cout << c;
    return 0;
}
