#include <iostream>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n, cnt = 0;
    string str;

    cin >> n >> str;

    for (int i = 0; i < n / 2; i++) {
        if (str[i] != str[n - i - 1]) cnt++;
    }

    cout << cnt;
    return 0;
}
​
