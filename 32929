#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n; cin >> n;
    
    if (n % 3 == 1) cout << "U";
    else if (n % 3 == 2) cout << "O";
    else cout << "S";
    return 0;
}

