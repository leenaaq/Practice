#include <iostream>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n, a, sum = 1; cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> a;
        sum += a;
        sum--;
    }

    cout << sum;
    return 0;
}
