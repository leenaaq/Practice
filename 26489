#include <iostream>
#include <string>
using namespace std;

int main(){
	ios::sync_with_stdio(0);
    cin.tie(0);
	
	string str;
	int count = 0;
	while(getline(cin, str)) count++;
	cout << count;

    return 0;
}


