#include <iostream>

int main()
{
    int n, sum = 0;
    std::cin >> n;
    std::string a;
    std::cin >> a;

    for (int i = 0; i < n; i++) {
        sum += int(a[i]) - 64;
    }
    std::cout << sum;
    return 0;
}
