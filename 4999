#include <iostream>
using namespace std;

void ah(void) {
	string a, b; cin >> a >> b;
	if (b.length()-1 > a.length()-1) cout << "no";
	else cout << "go";
}

int main(void) {
	ios::sync_with_stdio(false);
	cin.tie(nullptr); cout.tie(nullptr);

	ah();
	return 0;
}
