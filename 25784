#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int a[3];
        for (int i = 0; i < 3; i++) cin >> a[i];
    sort(a, a + 3);

    if (a[0] + a[1] == a[2]) cout << 1;
    else if (a[0] * a[1] == a[2]) cout << 2;
    else cout << 3;
    return 0;
}
