#include <iostream>

int main()
{
    int a, b;
    std::cin >> a;
    for (int i = 0; i < a; i++) {
        std::cin >> b;
        if (b >= 10) b = (b + 5) / 10 * 10;
        if (b >= 100) b = (b + 50) / 100 * 100;
        if (b >= 1000) b = (b + 500) / 1000 * 1000;
        if (b >= 10000) b = (b + 5000) / 10000 * 10000;
        if (b >= 100000) b = (b + 50000) / 100000 * 100000;
        if (b >= 1000000) b = (b + 500000) / 1000000 * 1000000;
        if (b >= 10000000) b = (b + 5000000) / 10000000 * 10000000;
        if (b >= 100000000) b = (b + 50000000) / 100000000 * 100000000;
        std::cout << b << "\n";
    }
    return 0;
}
