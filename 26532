#include <iostream>
#include <cmath>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int w, l; cin >> w >> l;
    int a = w * l;
    double result = ceil(a / 4840.0 / 5.0);
    cout << result;

    return 0;
}
