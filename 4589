#include <iostream>
#include <vector>
using namespace std;

int main() {
	int n;
	int a[3];

	cin >> n;
	cout << "Gnomes:" << "\n";

	while (n--) {
		for (int i = 0; i < 3; i++) cin >> a[i];
		if ((a[1] - a[0]) * (a[2] - a[1]) > 0) cout << "Ordered\n";
		else cout << "Unordered\n";

	}
	return 0;
}
