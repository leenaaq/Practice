#include <iostream>
#include <string>
using namespace std;

int main() {
	int n; cin >> n;
	string a = "";
	for (int i = 1; i <= n; i++) {
		a += to_string(i);
	}

	int idx = a.find(to_string(n));
	cout << idx + 1;
	return 0;
}
