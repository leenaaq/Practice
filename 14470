#include <iostream>

int main() {
    int a, b, c, d, e;
    std::cin >> a >> b >> c >> d >> e;
    if (a < 0) std::cout << -1 * a * c + d + e * b << '\n';
    else if (a > 0) std::cout << e * (b - a) << '\n';
    else std::cout << d + e * b << '\n';
}
