#include <iostream>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t, v, e; cin >> t;
    while (t--) {
        cin >> v >> e;
        cout << (v - e - 2) * -1 << "\n";
    }
    return 0;
}
