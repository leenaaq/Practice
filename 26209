#include <iostream>
using namespace std;

int main() {

  bool a = false;
  for (int i = 0; i < 8; i++) {
    int bit; cin >> bit;

    if (bit == 9) a = true;
  }

  if (a) cout << "F";
  else cout << "S";

  return 0;
}
